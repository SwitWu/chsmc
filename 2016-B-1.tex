\documentclass{chsmc}
\chsmcsetup{
	year = 2016,
	part = 1,
	type = B,
	show-answers = false,
}
\begin{document}

\section{填空题：本大题共 8 小题，每小题 8 分，共 64 分.}

\begin{enumerate}
	\item 等比数列 $\{a_n\}$ 的各项均为正数，且
		$a_1a_3 + a_2a_6 + 2a_3^2 = 36$，则 $a_2 + a_4$ 的值为 \blankline
	\item 设 $A = \{a\mid -1 \leqslant a \leqslant 2\}$，则平面点集
		$B = \{(x,y)\mid x,y \in A,\, x+y \geqslant 0\}$ 的面积为 \blankline
	\item 已知复数 $z$ 满足 $z^2 + 2z = \overline{z} \neq z$（$\overline{z}$ 表示 $z$ 的共轭复数），
		则 $z$ 的所有可能值的积为 \blankline
	\item 已知 $f(x)$, $g(x)$ 均为定义在 $\mathbf{R}$ 上的函数，$f(x)$ 的图象关于直线 $x = 1$ 对称，
		$g(x)$ 的图象关于点 $(1,-2)$ 中心对称，且
		$f(x) + g(x) = 9^x + x^3 + 1$，则 $f(2)g(2)$ 的值为 \blankline
	\item 将红、黄、蓝 $3$ 个球随机放入 $5$ 个不同的盒子 $A$, $B$, $C$, $D$, $E$ 中，
		恰有两个球放在同一盒子的概率为 \blankline
	\item 在平面直角坐标系 $xOy$ 中，圆
		$C_1\colon x^2 + y^2 - a = 0$ 关于直线 $l$ 对称的圆为
		$C_2\colon x^2 + y^2 + 2x - 2ay + 3 = 0$，则直线 $l$ 的方程为 \blankline
	\item 已知正四棱锥 $V$-$ABCD$ 的高等于 $AB$ 长度的一半，$M$ 是侧棱 $VB$ 的中点，
		$N$ 是侧棱 $VD$ 上的点，满足 $DN = 2VN$，则异面直线 $AM$, $BN$ 所成角的余弦值为 \blankline
	\item 设正整数 $n$ 满足 $n \leqslant 2016$，且
		$\left\{\dfrac{n}{2}\right\} + \left\{\dfrac{n}{4}\right\} + \left\{\dfrac{n}{6}\right\} + \left\{\dfrac{n}{12}\right\} = 3$.
		这样的 $n$ 的个数为 \blankline 这里 $\{x\} = x-[x]$，其中 $[x]$ 表示不超过 $x$ 的最大整数.
\end{enumerate}

\section{解答题：本大题共 3 个小题，满分 56 分. 解答应写出文字说明、证明过程或演算步骤.}

\begin{enumerate}[resume]
	\item (本题满分 16 分) 已知 $\{a_n\}$ 是各项均为正数的等比数列，且 $a_{50}$, $a_{51}$ 是方程
		$100\lg^2 x = \lg(100x)$ 的两个不同的解，求 $a_1a_2\cdots a_{100}$ 的值.
	\item (本题满分 20 分) 在 $\triangle ABC$ 中，已知
		$\overrightarrow{AB}\cdot\overrightarrow{AC} + 2\overrightarrow{BA}\cdot\overrightarrow{BC} = 3\overrightarrow{CA}\cdot\overrightarrow{CB}$.
		\begin{enumerate}[(1)]
			\item 将 $BC$, $CA$, $AB$ 的长分别记为 $a$, $b$, $c$，证明：$a^2 + 2b^2 = 3c^2$;
			\item 求 $\cos C$ 的最小值.
		\end{enumerate}
	\item (本题满分 20 分) 在平面直角坐标系 $xOy$ 中，双曲线 $C$ 的方程为 $x^2 - y^2 = 1$.
		求符合以下要求的所有大于 $1$ 的实数 $a$：过点 $(a,0)$ 任意作两条互相垂直的直线
		$l_1$ 与 $l_2$，若 $l_1$ 与双曲线 $C$ 交于 $P$, $Q$ 两点，$l_2$ 与 $C$ 交于 $R$, $S$ 两点，
		则总有 $|PQ| = |RS|$ 成立.
\end{enumerate}

\end{document}
